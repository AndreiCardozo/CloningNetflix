import { StyleSheet } from 'react-native';
import { THEME } from '../../theme';

export const styles = StyleSheet.create({
    container: {
        marginLeft: 8,
        height: 250,

    },
    cover: {
        height: 188,
        width: 120,
        borderRadius: 4,
    },

});